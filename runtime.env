# https://immich.app/docs/install/environment-variables/

# Immich
TZ=America/New_York
IMMICH_VERSION=release
IMMICH_ENV=production

# Postgresql
DB_HOSTNAME=localhost
DB_USERNAME=immich
# install.sh replaces DB_PASSWORD from the private password file created by
# pre-install.sh. If you are setting this up manually, replace the placeholder
# below with the password of your PostgreSQL 'immich' role. If it contains '$',
# '!', or other shell-special characters, keep the single quotes.
DB_PASSWORD='A_SEHR_SAFE_PASSWORD'
DB_DATABASE_NAME=immich
# VectorChord is now the recommended vector extension (v1.133.0+)
DB_VECTOR_EXTENSION=vectorchord

# Redis
REDIS_HOSTNAME=localhost

# Machine learning
# install.sh rewrites this default to match INSTALL_DIR on first setup.
MACHINE_LEARNING_CACHE_FOLDER=/home/immich/ml-models
